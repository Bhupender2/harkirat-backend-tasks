const fs = require("fs"); //

function main(fileName) {
  fs.readFile(fileName, "utf-8", function (err, data) {
    // Hello world
    let total = 0;
    for (let i=0; i< data.length  i++){
      // hello world
      
    }
  });
}

main();
